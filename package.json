{
  "name": "clankercom",
  "productName": "ClankerCom",
  "version": "2.0.0",
  "description": "A local hub where AI agents from any platform talk to each other over MCP — plus live claude.ai conversations driven through an embedded browser.",
  "main": "main.js",
  "author": "SpookyPirate",
  "license": "MIT",
  "homepage": "https://github.com/SpookyPirate/clankercom",
  "repository": {
    "type": "git",
    "url": "https://github.com/SpookyPirate/clankercom.git"
  },
  "scripts": {
    "start": "electron .",
    "dev": "electron . --enable-logging",
    "check": "node scripts/check.js",
    "build:bridge": "esbuild mcp-bridge.js --bundle --platform=node --target=node20 --outfile=dist-bridge/bundle.cjs && pkg dist-bridge/bundle.cjs --targets node22-win-x64 --output dist-bridge/clankercom-bridge.exe",
    "build:app": "electron-packager . \"ClankerCom\" --platform=win32 --arch=x64 --out=dist --overwrite --ignore=^/dist --ignore=^/dist-bridge --ignore=^/docs --ignore=^/scripts --ignore=\"\\.md$\" --extra-resource=dist-bridge/clankercom-bridge.exe --icon=build/icon.ico",
    "build:zip": "powershell -NoProfile -Command \"Compress-Archive -Path 'dist/ClankerCom-win32-x64/*' -DestinationPath 'dist/clankercom-2.0.0-win-x64.zip' -Force\"",
    "build": "npm run build:bridge && npm run build:app && npm run build:zip"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "express": "^4.19.2",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@electron/packager": "^20.0.0",
    "@yao-pkg/pkg": "^6.20.0",
    "electron": "^31.0.0",
    "esbuild": "^0.28.0"
  },
  "pkg": {
    "scripts": "mcp-bridge.js",
    "targets": [
      "node22-win-x64"
    ],
    "outputPath": "dist-bridge"
  }
}
